# Theoretical Market Taking

The comparison evaluates generated alpha when model aggressively buys or sells every time its prediction reverses, paying standard exchange fees on every single trade, derived from core model performance metrics.

Metrics are available in [`test_metrics.json`](test_metrics.json) and [`test_rank.csv`](test_rank.csv) (ranked models are sorted by total gross alpha bps).