# Continual Regime Adaptation

The comparison evaluates whether model can adapt smoothly to changing market conditions (regimes) and retains the ability to predict in previously learnt conditions (backward transfer).

Metrics are available in [`test_metrics.json`](test_metrics.json) and [`test_rank.csv`](test_rank.csv) (ranked models are sorted by continual adaptation score).