# Latency Arbitrage

The comparison evaluates execution speed and latency. During market price movement, if model can compute and execute predictions fast enough to provide viable edge in trading.

Metrics are available in [`test_metrics.json`](test_metrics.json) and [`test_rank.csv`](test_rank.csv) (ranked models are sorted by competitive sniping score).